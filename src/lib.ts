import { THEMES, SERVICES, STAGES } from "./const";
import GimmeSatsProvider from "./components/GimmeSatsProvider";
import GimmeSats, { GimmeSatsJustBolt } from "./components/GimmeSats";
import Bolt from "./components/icons/Bolt";

const ICONS = {
  Bolt,
};

export {
  ICONS,
  THEMES,
  SERVICES,
  STAGES,
  GimmeSatsProvider,
  GimmeSats,
  GimmeSatsJustBolt,
};
